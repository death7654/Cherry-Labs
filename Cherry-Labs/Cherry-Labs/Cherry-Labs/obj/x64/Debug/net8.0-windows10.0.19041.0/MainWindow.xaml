<Window
    x:Class="Cherry_Labs.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Cherry_Labs"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:muxc="using:Microsoft.UI.Xaml.Controls"
    mc:Ignorable="d"
    Title="Cherry-Labs">
    
    <!-- Background Color -->
    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <!-- Main layout -->
    <Grid>
        <!-- Nav bar -->
        <muxc:NavigationView PaneDisplayMode="Auto" IsBackButtonVisible="Collapsed">
            <muxc:NavigationView.MenuItems>
                <muxc:NavigationViewItem Content="Chat" Tag="chat" Icon="Library" IsSelected="True"/>
                <!-- <muxc:NavigationViewItem Content="Video" Tag="video" Icon="Video"/> -->
            </muxc:NavigationView.MenuItems>
            
            <!-- Main Content -->
            <Grid>
                <Grid.RowDefinitions>
                    <!-- Chat message list -->
                    <RowDefinition Height="*" />
                    <!-- Chat message list -->
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <!-- Chat messages -->
                <ListView x:ConnectionId='2' x:Name="ChatList" Margin="9,10,9,50" VerticalAlignment="Stretch">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding}" TextWrapping="WrapWholeWords" FontSize="16"/>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>

                <!-- Chat input row -->
                <Grid Height="60" Margin="10,0,10,0" VerticalAlignment="Bottom">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="18*" />
                        <ColumnDefinition Width="1*" />
                        <ColumnDefinition Width="1*" />
                    </Grid.ColumnDefinitions>

                    <RichEditBox x:ConnectionId='3' x:Name="ChatInput"
                                                         
                 Grid.Column="0"
                 PlaceholderText="Enter your message..."
                 VerticalAlignment="Center"
                 Margin="0,0,0,0" />

                    <!-- Send Button -->
                    <Button x:ConnectionId='4' Grid.Column="1"
                    Height="33"
                           FontSize="8"
                        VerticalAlignment="Center"
                        ToolTipService.ToolTip="Attach File"
                                                      
                        Margin="5,0,0,0">
                        <FontIcon FontFamily="Segoe MDL2 Assets"
                            Glyph="&#xE16C;" Margin="-5,0,4,0"
                       />
                    </Button>

                    <Button x:ConnectionId='5' Grid.Column="2"
                        Content="➤"
                        Height="33"
                        FontSize="12"
                        VerticalAlignment="Center"
                                                
                        ToolTipService.ToolTip="Send"
                        Margin="5,0,0,0"/>
                   </Grid>

            </Grid>
        </muxc:NavigationView>
    </Grid>
</Window>

